#include "myHeader.h"

void aggregateReco(const std::string accInputPath,const bool verbose)
{
    //still building
}

void buildAcceptance(const std::string accInputPath,const bool verbose)
{
    aggregateReco(accInputPath,verbose);
}